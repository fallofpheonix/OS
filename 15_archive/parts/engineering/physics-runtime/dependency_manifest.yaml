dependencies:
  external:
    - numpy (potential)
    - scipy (potential)
    - torch (potential)
  internal:
    - math-engine (P2)
  policy:
    CLONE_ON_DEMAND: true
    CACHE_TEMP: true
    INSTALL_ONLY: true
