# Pattern Library

Top-level pattern library for extracted rendering, control-plane, and runtime patterns.

Subfolders:
- `rendering-patterns/`
- `runtime-patterns/`
- `control-plane-patterns/`
- `shader-patterns/`

Populate with short notes and small reimplementations (no forks).
