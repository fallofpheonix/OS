# Kernel Implementation

Kernel and OS implementation root.

Owns boot, memory, scheduler, IPC, VFS, drivers, hooks, sandboxing, and prototypes.

Long-term custom OS work belongs here. Linux distribution work belongs in `11_distribution/`.

