# Labs

- Security ISO: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Tool group policy: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Package signing: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Installer path: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Hardening baseline: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Payload curation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Live image constraints: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
