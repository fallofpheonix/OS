# Labs

- eBPF tracing: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- kprobes/uprobes/tracepoints: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Syscall telemetry: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Auditd comparison: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Phoenix Trace: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Falco/Tracee model: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Perf overhead: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- BPF verifier safety: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
