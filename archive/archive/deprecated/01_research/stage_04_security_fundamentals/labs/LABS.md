# Labs

- Memory corruption: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- ROP/JOP: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- ASLR/DEP/NX: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Kernel attack surface: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Access control: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Capabilities: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Sandboxing: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Crypto basics: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Fuzzing: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Static analysis: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
