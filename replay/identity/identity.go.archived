package identity

type ReplayIdentity struct {
    Hash string

    SessionID string

    Active bool
}

func (r *ReplayIdentity) EndSession() {
    r.Active = false
}

func (r *ReplayIdentity) CheckDivergence(
other ReplayIdentity,
) bool {
    return r.Hash != other.Hash
}