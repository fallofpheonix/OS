# Canonical Ecosystem Makefile
validate:
	./control-plane/build-validation/validate-all.sh

test:
	pytest # or cargo test, etc.

lint:
	ruff check .

typecheck:
	mypy .

contracts:
	# buf generate or similar

observability:
	docker-compose -f infrastructure/observability/docker-compose.yml up -d
