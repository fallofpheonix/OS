# Datasets

Dataset inventory and dataset payload location.

Rules:

- Keep provenance metadata with every dataset.
- Store hashes for immutable inputs.
- Separate raw, processed, synthetic, and training data.
- Do not commit sensitive, licensed, or malware payloads without an explicit policy decision.

