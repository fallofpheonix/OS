# Labs

- TCP/IP: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- DNS/DHCP: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- TLS/QUIC: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Packet capture: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- BPF filters: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Raw sockets: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Zeek logs: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Suricata rules: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- WireGuard/VPN: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Zero trust networking: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
