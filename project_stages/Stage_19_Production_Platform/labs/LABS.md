# Labs

- Platform reliability: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Production deployment: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Governance: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Compliance: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Data retention: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Release gates: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Performance budgets: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Multi-user operations: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
