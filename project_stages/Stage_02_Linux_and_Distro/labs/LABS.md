# Labs

- Linux From Scratch: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Arch-based ISO: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Kali-based ISO: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Buildroot bridge: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Package payload ecosystem: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Repository policy: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Toolchain pinning: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Release image validation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
