# Labs

- IOC storage: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- YARA/Sigma/Suricata repositories: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Feed ingestion: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- MISP/OpenCTI: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- MITRE ATT&CK mapping: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Enrichment rules: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Threat hunting context: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
