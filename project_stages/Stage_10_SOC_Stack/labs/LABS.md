# Labs

- SIEM deployment: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- EDR/XDR model: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Alert lifecycle: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Case management: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Risk scoring: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Wazuh: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Security Onion: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- TheHive: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- SOC event schema: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
