# Labs

- Disk acquisition: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Memory acquisition: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Chain of custody: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Artifact extraction: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Timeline reconstruction: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- RAM analysis: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Incident reports: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Alert-to-evidence correlation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
