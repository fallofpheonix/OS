# Labs

- AI IDS: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- ML malware detector: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- UEBA: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Security RAG: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- SOC assistant: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Detection engineering AI: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Adversarial ML: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Explainable security ML: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
