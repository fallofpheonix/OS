# Labs

- Boot process: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- UEFI/BIOS startup: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- ELF loading: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Memory management: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Virtual memory: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Process model: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Threads and scheduling: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- IPC: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Filesystem internals: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Syscalls: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Userspace model: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Driver model: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
