# Labs

- Container isolation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Kubernetes admission control: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- SBOM policy: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Cloud logging: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Runtime security: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Service mesh: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Container network detection: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Supply-chain security: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
