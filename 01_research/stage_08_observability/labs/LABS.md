# Labs

- Log pipeline: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Metrics pipeline: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Trace pipeline: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- OpenTelemetry: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- OpenSearch/ELK: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Grafana: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Retention policy: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Schema governance: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
