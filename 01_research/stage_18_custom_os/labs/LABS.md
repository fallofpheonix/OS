# Labs

- Freestanding kernel: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Bootloader path: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Physical memory manager: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Scheduler: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Interrupts: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Filesystem: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Device drivers: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Shell: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Custom kernel ABI: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
