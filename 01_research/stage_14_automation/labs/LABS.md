# Labs

- SOAR playbooks: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Shuffle/n8n: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Ansible response: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Alert enrichment automation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Human approval gates: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Incident response agents: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
