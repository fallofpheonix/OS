# Labs

- Feature engineering: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Supervised learning: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Unsupervised learning: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Anomaly detection: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Model evaluation: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Dataset provenance: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- ML pipeline: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Drift detection: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
