# Labs

- Static malware triage: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Dynamic malware analysis: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Reverse engineering: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Sandbox design: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- YARA rules: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Malware family taxonomy: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Memory inspection: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Network behavior extraction: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
