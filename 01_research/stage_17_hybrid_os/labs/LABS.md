# Labs

- Custom kernel patches: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- LFS/Buildroot base: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Linux-derived control plane: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Own userspace services: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Hybrid boot path: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Security OS runtime: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
