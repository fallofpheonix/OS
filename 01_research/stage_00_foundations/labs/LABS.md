# Labs

- C systems programming: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Assembly basics: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Computer architecture: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Data structures: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Allocators: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Bitmaps: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Ring buffers: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Structured logging: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Build toolchain: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- QEMU/GDB workflow: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Research organization: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Project scope: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
