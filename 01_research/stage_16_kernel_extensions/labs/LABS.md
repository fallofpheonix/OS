# Labs

- Kernel modules: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- LSM hooks: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Custom probes: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Driver extensions: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Security hooks: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Telemetry ABI: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.

- Kernel/user event bridge: create minimal reproducible lab, capture inputs, outputs, tool versions, and failure cases.
