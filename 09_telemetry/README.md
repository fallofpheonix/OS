# Telemetry Implementation

Telemetry implementation root.

Owns eBPF telemetry, traces, events, syscall/Phoenix Traces, pipelines, and collectors.

Policy and detection logic consuming telemetry belongs in `07_security/` or `06_ai/`.

